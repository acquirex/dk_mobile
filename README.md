# DK Hardware mobile homepage prototype

## Run locally

```bash
npm install
npm run dev
```

Open the local URL printed by Vite. Use `npm run build` for a Vercel-ready production build.

The current product/category photography uses temporary hosted images. Replace the image URLs in `src/main.jsx` with supplied DK Hardware asset URLs for production fidelity.
